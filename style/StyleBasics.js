import { StyleSheet } from 'react-native'

const stylebasics = StyleSheet.create({
    container:{
        flex: 1,
        justifyContent: 'center',
        //alignItems: 'center',
        //backgroundColor: 'lightgreen', //#3498db - lichtblauw
        //padding: 20,
    }
});

export default stylebasics;